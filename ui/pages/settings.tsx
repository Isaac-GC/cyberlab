import Layout from "../components/layout";

const HomePage = (): React.ReactElement => {
    return (
        <Layout>
            <h1>Settings Page</h1>
        </Layout>
    )
}

export default HomePage