import Layout from "../components/layout";

const HomePage = (): React.ReactElement => {
    return (
        <Layout>
            <h1>Labs Page</h1>
        </Layout>
    )
}

export default HomePage